# Каталог для опциональных встроенных медиа-ассетов (fallback).
# Сами медиафайлы в .gitignore; этот файл нужен, чтобы каталог
# существовал в репозитории — иначе Flutter падает на сборке,
# т.к. assets/media/ объявлен в pubspec.yaml.
